# server-health-monitor
Python server health monitoring tool checks CPU, memory, disk, load average, and service status with a 4-level alert system. Generates JSON reports automatically. No external dependencies.
